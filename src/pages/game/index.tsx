import { fingerprintAtom } from '@/util/atoms'
import { trpc } from '@/util/trpc'
import { useAtom, useAtomValue } from 'jotai'
import { useRouter } from 'next/router'
import { useEffect, useState } from 'react'

function GameRoot() {
  return <></>
}

export default GameRoot
